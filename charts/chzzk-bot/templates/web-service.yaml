apiVersion: v1
kind: Service
metadata:
  name: {{ include "chzzk-bot.fullname" . }}-web
  labels:
    {{- include "chzzk-bot.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chzzk-bot.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
