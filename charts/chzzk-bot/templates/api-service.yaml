apiVersion: v1
kind: Service
metadata:
  name: {{ include "chzzk-bot.fullname" . }}-api
  labels:
    {{- include "chzzk-bot.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chzzk-bot.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
